# Web Evidence Verification JSON Instruction

## Value

Return one JSON object with `packFingerprint`, `budgetFingerprint`, `status`, `answerDraft`, `supportedUnitRefs`, and `unresolvedFactKeys`.
Use `sufficient`, `insufficient`, or `conflicted` for `status`.
Copy references and fingerprints exactly from the input.
Return JSON only.
