# Execution Harness Policy Context Labels

## Purpose

Provide prompt-owned English labels for execution harness policy-context prompt variables.

## Value

policy_sources_title=[Execution Harness Runtime Policy Sources]
provided_in_user_prompt=provided in the user prompt
context_json_in_user_prompt=The requested JSON decision context is provided in the user prompt.

## Out Of Scope

- This module does not own execution decision rules, allowed actions, runtime policy content, provider calls, JSON parsing, or route validation.
