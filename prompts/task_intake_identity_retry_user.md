The previous identity answer failed trusted-value validation with reason `{{reasonCode}}`.
Regenerate the complete task-intake JSON. Use the trusted identity context already provided. Make `identity_claim.claimed_name` and the candidate user message state the same correct name. Do not repeat the rejected name.
