# Web Source Selection JSON Instruction

## Value

Return one JSON object with `snapshotFingerprint`, `budgetFingerprint`, and `selections`.
Each selection must contain only `candidateRef`, `relevanceScore`, `reason`, and `factKeys`.
Copy references and fingerprints exactly from the input.
Return JSON only.
