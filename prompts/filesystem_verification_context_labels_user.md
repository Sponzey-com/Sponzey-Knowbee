# Filesystem Verification Context Labels

## Purpose

Provide prompt-owned English labels for filesystem verification runtime context blocks.

## Value

mutation_paths_header=[mutation_paths]

## Out Of Scope

- This module does not own filesystem verification instructions, original requests, mutation path values, path inference, evidence checks, or user-facing verification messages.
