# Knowbee 프로젝트 목표와 개발 방향

## 1. 프로젝트 목표

Knowbee(노비)는 사용자의 요청을 단순히 해석하거나 안내하는 데서 멈추지 않고, 실제 해결 방법을 찾아 실행하고 결과를 검증하는 에이전트 플랫폼이다.

Knowbee의 최상위 목표는 다음과 같다.

- 사용자의 요청과 의도를 정확히 이해한다.
- LLM을 통해 실행 가능한 해결 방법을 찾는다.
- 사용할 수 있는 기능과 연결된 실행 수단을 동원하여 실제 작업을 수행한다.
- 결과가 요청을 충족하는지 LLM을 통해 검증한다.
- 실패하면 이유만 보고하지 않고 다른 해결 방법을 찾아 다시 실행한다.
- 성공한 결과 또는 더 진행할 수 없는 명확한 근거만 사용자에게 전달한다.
- 사용자가 요청을 수정, 중단하거나 취소할 수 있는 통제권을 실행 전 과정에서 보존한다.
- 자율 실행은 안전, 권한, 개인정보, 비용과 명시된 사용자 제약 안에서만 수행한다.

## 2. 기본 실행 루프

모든 사용자 요청은 다음 순서로 처리한다.

### 2.0 세 가지 필수 LLM 판단

Knowbee의 실행 루프에는 다음 세 가지 LLM 판단이 반드시 존재해야 한다.

1. **문제 분석:** LLM이 사용자 요청의 의미, 실제 목표, 제약, 필요한 정보, 위험과 완료 조건을 진단한다.
2. **절차 수립:** LLM이 진단 결과와 현재 사용 가능한 능력을 바탕으로 실행 단계, 단계별 수단, 예상 결과와 검증 방법을 구조화한다.
3. **결과 검증:** LLM이 원 요청, 완료 조건과 실제 실행 근거를 함께 분석해 성공, 부분 성공, 미완료, 사용자 입력 필요 또는 차단을 판정하고 다음 행동을 선택한다.

세 판단은 서로 다른 입력과 출력 책임을 가지며 생략하거나 하나로 축약하지 않는다. 단순 대화도 사용자 답변을 생성하는 LLM 판단을 거치며, 도구를 사용한 작업은 결과 검증 없이 완료할 수 없다.

코드 하네스는 LLM 판단의 의미를 키워드, 정규식, 도메인별 숫자 추출 또는 고정 분류표로 대신하지 않는다. 하네스는 LLM 출력의 스키마, 필수 필드, evidence 참조, 출처 연결, 상태 전이, 대상, 권한, 안전과 정책 준수만 결정적으로 검증한다. 어댑터는 외부 결과를 수집하고 출처와 시각을 보존하며 표준 evidence 형식으로 변환하지만, 그 결과가 사용자 요청을 충족하는지는 판정하지 않는다.

LLM 내부에서 사용하는 request diagnosis, solution plan, result diagnosis, structured request, evidence envelope와 WorkRecord 원문은 내부 실행 데이터다. 이 데이터는 사용자 답변, 일반 API, 일반 WebUI, WebSocket, Product Log, Field Debug Log 또는 일반 내보내기에 노출하지 않는다. 외부에는 LLM이 생성한 사용자용 요약, 필요한 결과, 상태와 허용된 근거만 전달한다. 내부 구조 원문 조회는 명시적인 Audit 화면 또는 Audit API에서만 허용하며, 조회 주체, 시각, 대상 run/work와 목적을 감사 기록으로 남긴다. Audit에서도 secret, token, 개인정보, 내부 프롬프트 원문과 불필요한 raw payload는 계속 마스킹한다.

문제 분석 receipt가 없으면 절차를 확정할 수 없고, 절차 receipt와 정책 검증이 없으면 실행할 수 없으며, 결과 검증 receipt가 없으면 성공 또는 최종 완료로 전이할 수 없다. 스키마가 잘못되거나 근거가 부족한 LLM 판단은 임의의 코드 fallback으로 대체하지 않고 LLM에 구조 보정 또는 changed-strategy 재분석을 요청한다.

### 2.1 요청 접수

- 사용자의 원문 요청, 목적, 제약, 원하는 결과, 명시적으로 지정한 실행 방법을 보존한다.
- 이전 대화와 메모리는 현재 요청에 필요한 범위만 사용하며, 현재 사용자의 최신 지시가 이전 계획과 충돌하면 최신 지시를 우선한다.
- 요청 접수 시 성공 조건뿐 아니라 금지된 행동, 허용된 대상, 전달 위치와 승인이 필요한 부작용을 구분한다.
- 결과를 바꿀 정도의 필수 정보가 없을 때만 짧고 구체적으로 확인한다.
- 시스템이 직접 확인할 수 있는 정보나 실행 결과를 사용자에게 대신 제공하라고 요구하지 않는다.

### 2.2 LLM 문제 분석과 해결 절차 수립

- 요청의 의미와 완료 조건은 LLM이 분석한다.
- LLM은 작업을 실행 가능한 단계로 나누고 각 단계에 필요한 수단과 검증 방법을 정한다.
- 문제 진단과 실행 절차는 각각 구조화된 필드로 기록한다. 절차는 진단에서 확인된 목표, 제약과 완료 조건을 참조해야 하며, 원 요청의 일부를 누락하거나 새로운 목표를 임의로 추가할 수 없다.
- 자연어 키워드, 정규식 또는 고정된 의미 분류표로 해결 경로를 결정하지 않는다.
- 분석 결과는 장황한 내부 문장이 아니라 목표, 단계, 선택 수단, 완료 조건, 사용자 제약, 예상 부작용, 위험, 필요한 승인, 검증 방법과 다음 행동을 가진 구조화된 실행 계약으로 만든다.
- 단순 대화나 LLM 지식만으로 완결되는 질문은 불필요한 도구 호출이나 복잡한 워크플로우를 만들지 않고 하나의 직접 응답 단계로 처리한다.
- LLM이 만든 계획은 실행 명령이 아니라 실행 후보이다. 하네스가 스키마, 대상 존재 여부, 연결, 권한, 안전과 승인 조건을 검증한 뒤에만 실행한다.

### 2.3 해결 방법 실행

- 분석한 단계를 실제로 실행한다.
- 실행 가능한 수단은 Knowbee 자체 능력, 서브 에이전트, 연장(Yeonjang), Skills, MCP, 내장 도구와 허용된 외부 API를 포함한다.
- Knowbee 내장 `web_search`와 `web_fetch`는 공개 웹 정보가 필요한 요청을 해결하기 위한 기본 실행 수단이다. LLM은 현재 실행의 capability snapshot에 실제로 제공된 도구만 계획하며, `web_search`로 DuckDuckGo 검색 후보를 확보하고 `web_fetch`로 선택한 공개 문서를 Markdown evidence로 조회한다.
- 검색 또는 조회 호출의 성공만으로 사용자 목표가 달성되었다고 판정하지 않는다. LLM은 원 요청의 완료 조건, 실제 검색 결과와 문서 내용, 출처, 기준 시각, 수집 시각, 충돌 및 누락을 검증한 뒤 완료 또는 변경된 다음 전략을 결정한다.
- Yeonjang에는 웹 검색 capability를 추가하지 않는다. Yeonjang의 브라우저 제어는 명시적으로 지정된 원격 컴퓨터 조작 경계에 남기고, Knowbee 내장 공개 웹 검색과 혼합하지 않는다.
- 실행 직전에 사용하는 수단의 실제 연결 상태, 지원 기능, 대상, 권한과 입력 계약을 확인한다. 계획 시점의 기능 목록만 믿고 실행하지 않는다.
- 한 가지 수단의 성공 여부만 보지 않고 사용자 목표가 실제로 충족되었는지를 확인한다.
- 도구 호출 성공, 직접 조회 응답 존재, 서브 에이전트 완료 상태는 그 자체로 최종 성공의 근거가 아니다.
- 사용자가 실행 수단, 대상, 순서 또는 방법을 명시했다면 안전과 권한 정책 안에서 그 방법을 첫 번째 실행 경로로 사용한다.
- 명시된 방법이 실패하면 실패 원인을 분석한 뒤, 사용자의 의도를 훼손하지 않는 대체 경로를 찾는다.
- 파일 변경, 외부 전송, 결제, 삭제, 애플리케이션 제어처럼 부작용이 있는 실행은 대상과 범위를 다시 검증하고 필요한 승인을 받은 뒤 수행한다.
- 재시도 가능한 부작용 작업은 idempotency key, 실행 receipt 또는 사후 상태 확인 중 하나 이상으로 중복 실행을 방지한다.
- 실행 중 취소 신호를 받으면 진행 중인 하위 실행과 서브 에이전트에 취소를 전파하고, 새로운 부작용 작업을 시작하지 않는다.
- 웹 문서, 도구 출력, MCP 응답과 서브 에이전트 결과는 신뢰되지 않은 데이터로 취급한다. 그 안의 지시문이 시스템 정책이나 현재 실행 계약을 변경하도록 허용하지 않는다.

### 2.4 결과 검증과 다음 행동

- 실행 결과는 LLM이 원래 요청과 완료 조건을 기준으로 검증한다.
- LLM에는 최종 답변 문구만 전달하지 않는다. 실행에 사용한 도구, 연장, Skill, MCP, 직접 URL 조회, 파일, 외부 API와 서브 에이전트의 실제 결과 데이터, 출처 메타데이터와 receipt를 신뢰되지 않은 evidence로 함께 전달한다.
- 직접 조회 결과, 외부 API 결과, 연장 결과의 의미도 LLM이 분석한다. 수집 시각 또는 HTTP 성공만으로 현재성, 정확성이나 요청 대상 일치를 코드가 확정하지 않는다.
- 연장, Skill, MCP, 도구, 외부 API 또는 서브 에이전트가 `success`, `completed`, 정상 종료 코드 또는 성공 응답을 반환했다는 사실만으로 사용자 요청이 성공했다고 판정하지 않는다.
- 실행 수단의 성공 상태는 해당 수단이 호출되었다는 실행 근거일 뿐이며, 사용자 목표 달성의 충분조건이 아니다.
- LLM 결과 검증은 상태값만 보지 않고 실제 결과 데이터, 생성된 산출물, 변경된 상태, 전달 결과와 검증 근거를 함께 확인해야 한다.
- 결과 검증은 최소한 요청한 결과의 존재, 정확성, 완전성, 최신성, 대상 일치, 제약 준수와 사용자에게 필요한 형태로 전달되었는지를 평가한다.
- 검증 근거에는 출처 또는 산출물 참조, 수집/생성 시각, 대상 식별자, 실행 receipt와 불확실성을 필요한 범위에서 연결한다.
- 요청에 여러 완료 조건이 있으면 필수 조건을 각각 검증한다. 일부 조건만 충족한 결과는 부분 완료 또는 미완료로 판정한다.
- 실행은 성공했지만 결과가 비어 있거나, 관련이 없거나, 오래되었거나, 검증할 수 없거나, 요청한 대상과 다르면 완료로 판정하지 않는다.
- 결과를 검증할 근거가 부족하면 성공을 추정하지 않고, 근거를 확보할 다음 실행 방법을 분석한다.
- 상태 변경 작업은 응답 문구가 아니라 실제 사후 상태를 다시 읽어 검증한다. 사후 상태를 확인할 수 없다면 검증되지 않은 실행으로 기록한다.
- 고위험 작업과 중요한 외부 변경은 실행을 계획하거나 수행한 동일 응답만으로 자기 승인하지 않는다. 독립된 검증 단계, 별도 검토 역할 또는 결정적 사후 검증을 사용한다.
- 서로 다른 출처가 충돌하면 임의로 하나를 확정하지 않고 최신성, 출처 신뢰도와 직접성을 비교한다. 충돌이 해소되지 않으면 불확실성을 보존한다.
- 완료 조건을 충족하면 성공 결과와 필요한 근거를 사용자에게 전달한다.
- 완료 조건을 충족하지 못하면 실패 원인, 누락된 결과, 사용한 방법을 구조화해 2.2의 해결 방법 분석으로 되돌린다.
- 재분석에서는 이전과 동일한 입력과 전략을 그대로 반복하지 않는다. 도구, 출처, 대상, 작업 분할, 실행 순서, 권한 요청 또는 검증 방법 중 하나 이상을 바꾼다.
- 부분적인 도구 성공이나 불완전한 답변을 최종 성공으로 처리하지 않는다.

기본 상태 전이는 다음과 같다.

```text
REQUEST_RECEIVED
  -> SOLUTION_ANALYZED
  -> POLICY_VALIDATED
  -> EXECUTING
  -> RESULT_REVIEW
      -> SUCCEEDED -> USER_REPORT
      -> PARTIALLY_SUCCEEDED -> SOLUTION_ANALYZED | USER_REPORT
      -> INCOMPLETE -> SOLUTION_ANALYZED
      -> USER_INPUT_REQUIRED -> USER_CLARIFICATION
      -> BLOCKED -> USER_REPORT
      -> EXHAUSTED -> USER_REPORT
  -> CANCELLED -> USER_REPORT
```

이 상태 계약은 여러 실행, 재시도, 승인 또는 부작용이 있는 작업에 적용한다. 단순 대화와 한 번의 안전한 읽기 작업은 같은 의미의 순차 함수로 처리하며 불필요한 상태머신을 만들지 않는다.

## 3. 실행 수단 선택 원칙

### 3.1 사용자 지정 방법 우선

사용자가 특정 수단이나 대상을 명시하면 다음 순서를 적용한다.

1. 명시한 방법이 사용 가능하고 허용되는지 확인한다.
2. 사용 가능하면 해당 방법을 가장 먼저 실행한다.
3. 실패하면 결과와 실패 원인을 LLM이 진단한다.
4. 같은 목적을 달성할 다른 방법을 찾는다.
5. 대체 방법이 사용자 의도, 안전 또는 권한에 영향을 주면 실행 전에 필요한 확인을 받는다.

사용자 지정 방법은 안전, 개인정보, 권한, 승인, 법적 제한을 우회할 수 없다.

사용자가 특정 방법만 사용하라고 제한했다면 허가 없이 다른 방법으로 전환하지 않는다. 지정한 방법으로 완료할 수 없을 때는 확인된 이유와 최소 대안을 제시하고 사용자 결정을 기다린다.

### 3.2 가용 수단의 능동적 활용

사용자가 방법을 지정하지 않았다면 LLM은 현재 런타임에서 실제로 사용할 수 있는 수단을 비교하여 선택한다.

- Knowbee 자체 LLM 추론과 대화
- 현재 에이전트에 연결된 도구
- 역할과 기능이 적합한 서브 에이전트
- 연결된 연장과 해당 컴퓨터의 허용된 기능
- 활성화된 Skills
- 에이전트별로 독립 연결된 MCP
- 정확한 URL의 직접 조회 및 허용된 외부 API
- 파일, 명령, 애플리케이션 또는 장치 조작 수단

카탈로그에 존재한다는 사실과 현재 실행 가능하다는 사실을 구분한다. 연결 상태, 대상, 권한, 입력 계약을 확인한 수단만 실행한다.

수단 선택은 편의적인 고정 순서가 아니라 다음 조건을 구조적으로 비교한다.

- 사용자 지정 여부와 역할 적합성
- 현재 연결 및 실행 가능 여부
- 필요한 권한과 예상 부작용
- 결과를 검증할 수 있는 근거의 품질
- 개인정보 노출, 외부 전송과 비용 범위
- 이미 실패한 전략과 실질적으로 다른지 여부

장치와 컴퓨터 제어 요청의 실행 수단은 다음 우선순위 계약을 추가로 지킨다.

1. 사용자가 지정한 exact 대상과 방법을 먼저 검증하고 사용한다.
2. 지정 방법이 없으면 LLM이 요청한 효과를 직접 수행하는 purpose-specific capability를 선택한다. 연결된 연장이 그 capability를 제공하면 해당 연장 capability가 첫 실행 경로다.
3. 선택된 purpose-specific capability의 연결, 대상, 권한 또는 지원 상태가 부족하면 그 상태를 typed evidence로 진단하고, 같은 capability를 제공하는 다른 허용 대상 또는 사용자가 허용한 대체 방법을 검토한다.
4. `shell_exec`, 범용 프로세스 실행, 임의 OS 명령 조합은 purpose-specific capability의 구현 대체물로 사용하지 않는다. 사용자가 shell을 명시했거나 LLM이 현재 capability 계약에 전용 기능이 없음을 확인하고 exact shell capability를 별도 방법으로 선택한 경우에만 계획 후보가 될 수 있다.
5. 전용 capability와 범용 executor를 같은 우선순위 후보 집합에 넣어 LLM이 임의로 우회하게 하지 않는다. 구조화 진단이 선택한 exact method와 필수 전달 capability만 해당 실행 scope에 admission한다.
6. 전용 capability 실행 실패 후 범용 executor로 전환하면 대상, 부작용, 권한 및 승인 범위가 달라지므로 기존 승인을 재사용하지 않고 새로운 전략과 승인을 요구한다.

따라서 카메라 촬영은 `yeonjang_camera_capture`, 화면 캡처는 해당 화면 캡처 capability처럼 장치 효과를 직접 소유한 계약을 사용한다. `imagesnap`, `ffmpeg`, Python/OpenCV 또는 임의 shell 명령을 찾아 조합하는 것은 전용 연장 capability가 존재하는 요청의 정상 경로나 자동 복구 경로가 아니다.

### 3.3 연장(Yeonjang)의 정의와 실행 경계

연장(Yeonjang)은 노비(Knowbee) 본체가 설치되지 않은 원격 컴퓨터 또는 별도 컴퓨터 인스턴스를 노비가 제어하기 위한 cross-platform 실행 런타임이다. 연장은 Linux, Windows, macOS를 지원 대상으로 하며, 각 운영체제의 권한 모델과 시스템 API 차이를 어댑터 내부에 캡슐화한다.

연장은 에이전트가 아니며, 독립적으로 사용자 의도를 판단하거나 최종 답변을 생성하지 않는다. 연장은 노비가 LLM으로 분석하고 하네스가 검증한 실행 계약에 따라 대상 컴퓨터에서 실제 작업을 수행하고, 실행 결과와 근거를 반환한다.

연장은 다음 기능 범주를 지원해야 한다.

- **파일과 디스크 접근:** 대상 컴퓨터의 허용된 경로 안에서 파일 읽기, 파일 쓰기, 파일 수정, 파일 삭제, 디렉터리 목록 조회, 파일 검색, 파일 메타데이터 확인과 저장 공간 상태 확인을 수행한다.
- **카메라 접근:** 대상 컴퓨터에 연결된 카메라 목록, 권한 상태, 사용 가능 상태를 확인하고, 허용된 경우 사진 캡처 또는 영상 입력 확인을 수행한다.
- **화면과 입력 제어:** 대상 컴퓨터의 화면 캡처, 화면 텍스트 확인, 마우스 이동/클릭, 키보드 입력, 단축키 입력을 수행한다.
- **브라우저와 프로세스 접근:** 대상 컴퓨터에서 실행 중인 브라우저와 일반 프로세스의 존재, 상태, 창 정보, 실행 가능 여부를 확인하고, 허용된 범위에서 애플리케이션 실행, 포커스 전환, 프로세스 상태 조회와 종료 요청을 수행한다.
- **로컬 명령 실행:** 대상 컴퓨터에서 허용된 명령만 실행하고, 표준 출력, 표준 오류, 종료 코드, 실행 시간과 작업 디렉터리를 evidence로 반환한다.
- **기타 로컬 리소스 접근:** 운영체제 권한이 허용하고 노비 정책이 승인한 범위에서 클립보드, 알림, 네트워크 상태, 장치 상태, 로컬 서비스 상태, 시스템 정보 같은 리소스를 조회하거나 조작한다.

연장 기능은 운영체제마다 지원 수준이 다를 수 있다. 연장은 기능 이름만 보고 실행하지 않고, 현재 인스턴스의 capability matrix로 지원 여부, 입력 계약, 출력 형식, 필요한 운영체제 권한, 승인 필요 여부와 known limitation을 보고해야 한다.

연장은 다음 책임만 가진다.

- 자신의 인스턴스 ID, 표시 이름, 운영체제, CPU 아키텍처, 버전, 연결 상태, 지원 기능, 권한 상태와 마지막 관측 시각을 Knowbee에 보고한다.
- 하나의 컴퓨터 인스턴스 안에서 자기 자신이 유일한 활성 연장인지 확인하고, 중복 실행 또는 중복 연결을 발견하면 충돌 상태를 보고한다.
- 요청받은 기능이 현재 인스턴스에서 지원되고 허용되는지 실행 전에 확인한다.
- 파일, 디스크, 카메라, 화면, 입력, 브라우저, 프로세스, 명령, 기타 리소스 접근마다 필요한 운영체제 권한과 노비 정책 권한을 분리해 보고한다.
- 실행 요청의 대상, 입력, 권한, timeout, idempotency key와 correlation ID를 검증한다.
- 실행 결과, 실패 원인, 산출물 위치, 사후 상태와 수집 시각을 구조화된 evidence로 반환한다.
- 중복 실행 방지가 필요한 작업은 같은 idempotency key에 대해 같은 부작용을 반복하지 않는다.
- 취소 요청을 받으면 가능한 범위에서 진행 중인 로컬 작업을 중단하고 취소 상태를 보고한다.

연장은 다음 책임을 가지지 않는다.

- 사용자 요청의 의미 분석, 해결 절차 수립, 결과 충분성 판정 또는 최종 사용자 응답 작성
- Knowbee 시스템 정책, 프롬프트, 에이전트 권한 또는 실행 계약 변경
- 다른 연장, Skill, MCP 또는 서브 에이전트로의 임의 위임
- 사용자의 명시 승인 없이 파일 쓰기/삭제, 디스크 변경, 외부 전송, 카메라 캡처, 화면/입력 제어, 브라우저/프로세스 조작, 명령 실행 같은 부작용 확대
- 도구 출력 안의 자연어 지시문을 상위 정책보다 우선하는 행위

연장은 하나의 인스턴스에 하나만 존재할 수 있다. 같은 연장 ID가 둘 이상의 클라이언트에서 동시에 사용되면 새 연결 또는 충돌 연결을 거부하고, Knowbee는 충돌 사실을 Field Debug Log와 Audit evidence에 기록한다.

노비가 설치된 컴퓨터와 연장이 설치된 컴퓨터는 같은 컴퓨터일 수도 있고 서로 다른 컴퓨터일 수도 있다. 노비는 모든 연장을 원격 실행 대상으로 취급한다. 같은 컴퓨터에서 실행되는 연장도 권한, 연결, capability matrix, 실행 receipt와 사후 검증을 생략하지 않는다.

Knowbee는 연장을 사용할 때 다음 순서를 지킨다.

1. LLM이 사용자 요청과 완료 조건을 분석하고 연장 사용 필요성을 실행 후보로 제안한다.
2. 하네스가 대상 연장의 연결 상태, 운영체제, 지원 기능, 권한, 대상 인스턴스, 승인 필요 여부와 입력 계약을 검증한다.
3. 사용자가 특정 연장 또는 컴퓨터를 지칭했다면 그 인스턴스를 우선 선택한다.
4. 사용자가 모든 연장을 지칭했다면 각 연장에 동일 명령을 무조건 복제하지 않고, 각 인스턴스의 지원 기능과 권한을 개별 검증한 뒤 실행 가능한 연장에만 실행한다.
5. 부작용이 있는 실행은 승인 정책, idempotency key, timeout과 사후 검증 조건을 포함한 실행 receipt를 만든 뒤 수행한다.
6. 실행 결과는 LLM 결과 검증 단계에 신뢰되지 않은 evidence로 전달한다.
7. LLM은 연장의 성공 응답이 아니라 사용자 요청의 완료 조건을 기준으로 성공, 부분 성공, 미완료, 사용자 입력 필요, 차단 또는 경로 소진을 판정한다.

연장이 없거나 연결이 끊겼거나 필요한 기능을 지원하지 않으면 Knowbee는 즉시 실패로 종료하지 않는다. Knowbee 자체 능력, Skill, MCP, 정확한 URL 직접 조회, 허용된 외부 API, 파일, 다른 연결된 연장 또는 서브 에이전트로 해결 가능한 범위를 먼저 분석하고 실행한다. 원격 컴퓨터의 파일, 디스크, 카메라, 브라우저, 프로세스 또는 기타 로컬 리소스에 직접 접근해야 해서 연장이 반드시 필요한 작업만 사용자에게 짧고 정확하게 설명한다.

연장 관련 사용자 보고는 내부 실행 데이터와 비밀정보를 노출하지 않는다. 사용자에게는 대상 인스턴스, 수행한 작업, 확인된 결과, 실패 원인과 필요한 다음 조치만 전달한다. 원시 payload, 내부 capability matrix, token, secret, 내부 프롬프트와 구조화된 LLM 판단 원문은 Audit 모드에서만 제한적으로 확인할 수 있다.

## 4. 실패와 재귀적 해결

실패는 즉시 사용자 보고로 전환하는 종료 신호가 아니다. 실패는 다음 해결 방법을 찾기 위한 입력이다.

실패할 때마다 LLM은 다음을 진단한다.

- 어떤 완료 조건이 충족되지 않았는가
- 어떤 실행과 근거가 확보되었는가
- 실패 원인이 입력, 도구, 출처, 대상, 권한, 연결, 실행 순서 또는 검증 중 어디에 있는가
- 같은 실패를 피하기 위해 무엇을 바꿔야 하는가
- 다음 실행이 안전하고 실제로 가능한가

각 실행 시도는 사용한 수단, 입력, 대상, 전략, 결과, 실패 원인과 검증 결과를 구조화해 기록한다. 다음 해결 방법은 이 기록과 비교하여 이전 시도와 실질적으로 다른 경우에만 실행한다.

예를 들어 직접 URL 조회나 외부 API 응답에 필요한 값이 없으면 HTTP 성공 또는 adapter 성공으로 종료하지 않는다. 다른 정확한 URL, 공식 API, 연결된 Skill/MCP, 연장 또는 다른 실행 수단을 검토하고 실행한다.

재귀 실행은 목표를 달성할 가능성이 있는 서로 다른 방법이 남아 있는 동안 계속한다. 단순 횟수 제한만으로 실패를 선언하지 않는다.

재귀 실행이 무한 순환이나 통제되지 않은 비용으로 이어지지 않도록 다음을 지킨다.

- 같은 실패 원인과 같은 전략으로 돌아오는 순환을 감지한다.
- 시간, 토큰, 외부 비용 또는 실행 시간이 사용자나 런타임의 명시적 한계에 가까워지면, 이를 실패로 단정하지 않고 현재 근거와 다른 해결 후보를 다시 평가한다.
- 한계를 넘기 위해 사용자 승인이나 추가 자원이 필요하면 현재 진행 상황과 필요한 결정을 보고한다.
- 실행 가능한 해결 후보가 없다는 판정은 상상 가능한 모든 방법이 아니라 현재 권한, 연결, 정책과 확인된 기능 안에서 실질적으로 다른 후보가 없다는 뜻이다.
- 새로운 요청이 진행 중인 목표를 변경하면 기존 실행을 그대로 이어가지 않고 취소, 병합 또는 별도 실행 중 무엇인지 명시적으로 결정한다.

## 5. 종료와 사용자 보고

다음 조건 중 하나일 때 실행 루프를 종료한다.

- 요청의 완료 조건을 검증 가능한 근거로 충족했다.
- 사용자가 중단 또는 취소를 요청했다.
- 필요한 권한이나 승인이 거부되었다.
- 계속 진행하면 안전, 개인정보, 비용 또는 시스템 무결성 위험이 발생한다.
- 필수 외부 자원이나 연결이 없고, 현재 권한과 확인된 기능 안의 실질적으로 다른 대체 후보를 평가했지만 실행 가능한 경로가 없다.
- 추가 사용자 입력 없이는 서로 다른 안전한 해결 경로를 선택할 수 없다.
- 시스템이 확인할 수 있는 실질적으로 다른 해결 경로가 소진되었고, 그 판정 근거가 실행 기록에 남아 있다.

성공 보고에는 실제 결과와 필요한 근거만 포함한다. 실패 또는 차단 보고에는 다음만 간결하게 포함한다.

- 완료하지 못한 항목
- 확인된 직접 원인
- 이미 시도한 서로 다른 해결 경로
- 사용자가 제공해야 하는 최소 입력 또는 실행 가능한 다음 방법

가능한 방법이 남아 있는데 사용자에게 결과 조회나 작업 수행을 떠넘기지 않는다. `원하시면 계속하겠다`는 식으로 실행을 중단하지 않고, 승인이나 필수 입력이 필요한 경우가 아니라면 다음 방법을 바로 실행한다.

부분 완료를 보고할 때는 완료된 항목과 미완료 항목을 분리하고, 미완료 항목을 성공한 것처럼 표현하지 않는다. 사용자에게 표시되는 확인 질문, 진행 보고, 승인 요청, 부분 보고와 최종 보고의 모든 자연어 문장은 LLM 응답 계층에서 사용자의 요청 언어로 작성하되, 내부 원문 프롬프트, 비밀정보와 불필요한 실행 세부사항은 노출하지 않는다.

## 6. 개발 방향

### 6.1 LLM과 코드의 책임

- LLM은 문제 분석, 해결 절차 수립, 단계 분해, 실행 evidence의 의미 분석, 결과 충분성 검증, 실패 진단과 다음 행동 선택을 담당한다.
- 사용자 요청을 해결하기 위한 의미 판단을 시맨틱 알고리즘으로 대신하는 방식은 지양한다. 시맨틱 유사도, 임베딩 거리 임계값, 키워드, 정규식, 문자열 비교 또는 하드코딩된 자연어 분류를 사용해 요청 의미, 에이전트나 실행 수단 선택, 결과 충분성, 실패 원인 또는 재시도 전략을 결정하지 않는다. 이러한 판단은 LLM이 구조화된 컨텍스트와 evidence를 바탕으로 수행한다.
- 코드는 타입, 스키마, 상태 전이, 권한, 안전, 도구 연결, 실행, 영속화와 추적을 담당한다.
- 어댑터는 외부 시스템의 전송, 호출, 수집, 구조 변환과 provenance 보존만 담당한다. 어댑터가 자연어 또는 도메인 의미를 분석해 현재 값, 정답, 완료 여부를 판정하지 않는다.
- 하네스는 LLM 진단과 절차 및 결과 검증 receipt의 존재, 유효성, scope와 evidence binding을 검사한다. 하네스가 별도의 의미 알고리즘으로 LLM 판단을 대체하거나 승인하지 않는다.
- 코드는 자연어 의미를 키워드나 정규식으로 대신 판단하지 않는다.
- 모든 실행 단계는 명시적인 입력, 출력, 완료 조건과 실행 근거를 가진다.
- LLM 출력이 스키마에 맞지 않거나 필수 판단 근거가 없으면 하네스가 구조 보정을 요청한다. 같은 구조 오류가 반복되면 입력 정규화, 프롬프트, 모델 또는 검증 경로를 바꿔 유효한 계약을 다시 구한다.
- 유효한 실행 계약을 확보하지 못한 상태에서는 임의의 기본 행동을 실행하지 않는다. 실질적으로 다른 보정 경로가 없을 때만 안전한 미실행 상태를 유지하고 확인된 원인을 보고한다.
- LLM의 확신도는 참고 정보이며 실행 허가나 성공 판정 자체가 아니다. 권한, 정책과 결과 근거는 코드 경계에서 별도로 검증한다.

### 6.2 아키텍처

- Layered Architecture와 Clean Architecture의 의존성 방향을 지킨다.
- 도메인과 유스케이스는 특정 LLM, Skill, MCP, 연장, 데이터베이스 또는 UI 구현에 직접 의존하지 않는다.
- 외부 실행 수단은 포트와 어댑터 경계 뒤에 둔다. 연장은 로컬 또는 원격 컴퓨터 실행 어댑터로 취급하며, 도메인 계층은 MQTT, OS API, 프로세스, 장치 권한 또는 tray 구현에 직접 의존하지 않는다.
- 연장 구현은 cross-platform 포트와 운영체제별 어댑터를 분리한다. 파일/디스크, 카메라, 화면/입력, 브라우저/프로세스, 명령 실행과 기타 리소스 접근은 공통 capability 계약을 따르고, Linux, Windows, macOS 차이는 어댑터 내부에서만 처리한다.
- 연장 capability 계약은 기능 이름, 지원 여부, 입력 스키마, 출력 스키마, 권한 요구, 부작용 등급, 사후 검증 방법과 known limitation을 포함한다.
- 실행 루프는 하나의 추적 가능한 canonical 상태 계약을 사용하고 중복된 플래그 조합으로 표현하지 않는다.
- 런타임 설정은 시작 시 한 번 수신하고 이후 명시적 인자와 컨텍스트로 전달한다.
- 에이전트별 메모리와 Skill/MCP 연결은 격리하고, 에이전트 사이의 정보 이동은 명시적인 핸드오프 계약으로만 수행한다.
- 실행 계약, 정책 검증, 어댑터 실행, 결과 검증과 사용자 응답 생성을 서로 다른 책임 경계로 유지한다.
- 모든 시스템 프롬프트 원본은 영어로 관리하고, 사용자에게 보이는 응답 언어는 현재 요청의 언어로 결정한다.

### 6.3 에이전트와 실행 수단 경계

- 서브 에이전트가 존재한다는 이유만으로 위임하지 않는다. 역할 적합성, 연결 관계, 메모리 경계와 검증 필요성을 분석해 위임한다.
- 메인 에이전트는 구성된 최상위 서브 에이전트에게만 직접 위임하고, 하위 위임은 각 에이전트의 명시된 계층과 권한 안에서 수행한다.
- 서브 에이전트가 반환한 결과는 최종 사용자 답변이 아니다. 요청을 소유한 상위 에이전트가 완료 조건과 근거를 검증하고 필요한 경우 취합하거나 재위임한다.
- Skill과 MCP는 공통 카탈로그를 사용할 수 있지만 활성 연결, 자격 증명, 실행 컨텍스트와 메모리는 에이전트별로 격리한다.
- 연장이 없거나 대상 연장이 연결되지 않았으면 Knowbee 자체 능력과 다른 허용 수단으로 해결 가능한 범위를 먼저 수행한다. 연장이 반드시 필요한 나머지 작업만 확인된 이유와 함께 보고한다.
- 연장은 에이전트 이름 체계와 분리된 인스턴스 실행 대상이다. 사용자가 특정 컴퓨터 또는 연장을 지칭하면 Knowbee는 해당 연장 ID와 표시 이름을 확인한 뒤 정확한 대상에만 실행한다.
- 여러 연장에 같은 명령을 수행할 때는 각 연장의 지원 기능, 권한, 연결 상태와 사후 검증 결과를 개별 receipt로 기록하고, 전체 결과를 성공/부분 성공/실패로 취합한다.

### 6.4 구현과 검증

- TDD로 실패 사례와 완료 조건을 먼저 재현한다.
- Tidy First 원칙에 따라 구조 정리와 동작 변경을 가능한 한 분리한다.
- 단위 테스트는 분석 계약, 상태 전이, 수단 선택과 종료 판정을 검증한다.
- 통합 테스트는 LLM 분석부터 실제 도구 실행, 결과 검토, 재실행과 최종 전달까지 검증한다.
- 현재 정보 조회 테스트는 첫 번째 출처가 불완전할 때 다른 방법으로 전환하고 실제 값을 반환하는지 확인한다.
- 성공 상태와 빈 결과, 성공 상태와 잘못된 대상, 성공 상태와 오래된 데이터, 부분 결과와 전체 완료를 구분하는 회귀 테스트를 둔다.
- 연장, Skill, MCP, 도구와 서브 에이전트의 성공 receipt만 있고 요청한 결과가 없는 경우 실행 루프가 완료되지 않는지 검증한다.
- 연장 테스트는 연결 없음, 오래된 capability matrix, 권한 부족, 대상 인스턴스 불일치, 중복 연장 ID, timeout, 취소, 부분 성공과 사후 상태 불일치를 각각 재현한다.
- Linux, Windows, macOS별 연장 테스트는 같은 capability 계약으로 파일 읽기/쓰기, 디스크 상태 조회, 카메라 목록/캡처, 브라우저/프로세스 조회, 명령 실행과 권한 거부 결과가 일관된 evidence 형식으로 반환되는지 검증한다.
- 화면, 카메라, 입력 제어, 명령 실행과 파일/디스크 접근은 실행 전 권한 확인과 실행 후 결과 검증을 모두 통과해야 완료로 판정한다.
- 파괴적 또는 외부 부작용 작업의 재시도에서 중복 실행이 차단되는지 검증한다.
- 취소 전파, 사용자 최신 지시 우선, 특정 방법만 허용한 요청과 동시 요청 격리를 검증한다.
- 도구 결과 안의 지시문이 시스템 정책이나 실행 계약을 변경하지 못하는지 검증한다.
- 고위험 결과가 독립된 근거 없이 동일 실행자의 자기검증만으로 완료되지 않는지 검증한다.
- 실행 완료는 도구 성공이 아니라 사용자 요청의 완료 조건 충족으로 판정한다.

### 6.5 추적성과 로그

- 모든 요청은 요청 ID, 선택한 해결 방법, 실행 단계, 사용 수단, 결과 진단과 최종 상태를 추적할 수 있어야 한다.
- Product Log는 시작, 최종 성공, 실패와 사용자 영향만 기록한다.
- Field Debug Log는 실행 경로, 도구/에이전트 선택, 결과 진단과 전략 변경을 기록한다.
- Development Log는 구조화 계약, 상태 전이와 테스트 진단을 기록한다.
- 비밀정보, 내부 프롬프트 원문과 개인 메모리는 로그에 노출하지 않는다.

## 7. 완료 기준

이 개발 방향은 다음 조건을 만족할 때 구현된 것으로 본다.

- 사용자 요청이 LLM 기반 구조화 진단을 거쳐 실행된다.
- 사용자 지정 실행 방법이 허용 범위 안에서 첫 번째 경로로 선택된다.
- Knowbee, 서브 에이전트, 연장, Skills, MCP와 도구가 동일한 실행 계약 아래 선택될 수 있다.
- 연장은 에이전트가 아닌 cross-platform 원격 컴퓨터 실행 런타임으로 정의되며, 연결 상태, 운영체제, 지원 기능, 권한, 대상 인스턴스와 evidence receipt가 실행 전에 검증된다.
- 연장은 노비가 설치되지 않은 컴퓨터에서도 파일/디스크, 카메라, 화면/입력, 브라우저/프로세스, 명령 실행과 기타 로컬 리소스 접근을 공통 capability 계약으로 제공한다.
- 특정 연장, 특정 컴퓨터 또는 모든 연장 대상 실행은 대상 선택, 개별 실행 receipt와 사후 검증 결과를 남긴다.
- 한 수단의 실행 성공과 사용자 목표 완료가 별도로 검증된다.
- 실행 수단의 성공 receipt만으로 최종 상태가 `SUCCEEDED`가 되지 않는다.
- 실제 결과와 검증 근거가 모든 필수 완료 조건을 충족해야만 성공으로 종료된다.
- 불완전한 결과는 다른 전략을 가진 재분석과 재실행으로 이어진다.
- 부작용 작업은 승인, 중복 실행 방지와 사후 상태 검증을 통과한다.
- 신뢰되지 않은 외부 결과는 데이터로만 처리되며 시스템 정책이나 실행 계획을 직접 변경하지 않는다.
- 취소와 최신 사용자 지시가 진행 중인 실행과 하위 작업에 일관되게 반영된다.
- 실행 가능한 방법이 남아 있는 동안 단순한 변명이나 사용자 전가로 종료하지 않는다.
- 성공, 사용자 입력 필요, 안전 차단, 해결 경로 소진이 명시적인 최종 상태로 구분된다.
- 최종 사용자 응답은 LLM이 사용자의 요청 언어로 작성한다.
